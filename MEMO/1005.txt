------------------------------------------------------------------------------------------------------
github에서 download zip하거나 git clone URL 해서 다운받으면 됨~~ 
어제꺼 복습~~
상속은 다~~~~~~받는다. 소스 내용은 많아지겠지만 양은 작아짐. 
------------------------------------------------------------------------------------------------------
상속받을땐 extends 써서 받는다~
마우스 우클릭-generate 클릭하면 여러가지 기능 나옴.

this, super -- 둘다 주소값을 담는다.
this는 나 자신의 주소값
super는 내 (직속)부모의 주소값

this.??? > 멤버 필드, 멤버 메소드 
super.??? >>
this(); >> (나 자신의) 생성자 호출.
 super(); >>(부모의) 생성자 호출. -- 항상 첫줄에 있음.

this 생성자는 어떻게 사용할까? 기본값 지정인가...?
------------------------------------------------------------------------------------------------------
외부에서 값을 지정받지 않으면 기본생성자. 나머진 생성자.

생성자 쓰는이유?? 은닉화, 캡슐화 (멤버필드 숨겨져 있음 그래서 값을 바로 넣을수 없어)

★setter 메소드 이용해서 객체에 값을 넣었음
★생성자를 이용해서 객체에 값을 넣었음.

★★객체에 은닉화된 멤버필드에 값을 넣을 수 있는 방법은 딱 2가지! >> setter메소드, 생성자

★★객체에 은닉화된 멤버필드에 값을 빼내는 방법은 딱 1가지! >> getter 메소드

메소드와 생성자의 차이???? 

​메소드
동작 ,객체간의 데이터 전달 수단
외부로부터 매개값 받을수 있음
리턴값 있음 (void 라도있음) 

​생성자
필드를 초기화 하기 위함
메소드를 호출해서 사용준비
클래스이름과 똑같아야함
리턴타입이 없다. 

패키지 달라도 상속 관계면 protected라도 접근가능함.

다형성(여러가지 형태가 있는 성질)

1. 부모타입은 자식객체 주소값 저장할 수 있다.( 가리킬 수 있다.)
2. 자식타입은 부모객체 주소값 저장할 수 없다.(가리킬 수 없다.) 바로 컴파일에러 발생
3. 매소드 호출은 타입이 알고 있는 것만 호출 할 수 있고, 내용은 객체 기준이다.

--------------------------------------옴뇸뇸   밥먹자!-----------------------------------------------다형성 특성으로 동물울음소리 구현
static 안쓰면 객체화 해야하는데 new라는 단어 있어야함...
------------------------------------------------------------------------------------------------------
추상 클래스
텔레비전 클래스 - 인터페이스랑.
ㅇ
------------------------------------------------------------------------------------------------------
ArrayyListTest 알고리즘 관련문제 어질어질합니다~
------------------------------------------------------------------------------------------------------
other 패키지. ,,,,
------------------------------------------------------------------------------------------------------